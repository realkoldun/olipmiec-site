import { NotFoundPage } from '@/components/not-found-page';

/**
 * Страница 404 внутри группы (main)
 *
 * Отображается когда страница не найдена
 * Использует общий layout с Header и Footer
 */
export default function NotFound() {
  return <NotFoundPage />;
}
